import express from 'express';
import net from 'node:net';
import crypto from 'node:crypto';
import fs from 'node:fs';
import path from 'node:path';
import { getRuntimeConfig } from './db.js';

export const V251_COOKIE_FIRST_LOGIN_VERSION='2026-08-21-v251-cookie-first-login-v1';
const INSTALLED=Symbol.for('ce-qc.v251-cookie-first-login-installed');
const FAST_COOKIE='ce_v213_fast_session';
const AUTH_PORT=Math.max(1024,Math.min(65535,Number(process.env.CE_QC_AUTH_SIDECAR_PORT||5179)));
const APP_PATHS=new Set(['/','/home','/ce','/ceaf','/tbkh','/ali1688','/shopeecn','/shopeevn','/whpp','/tracking','/exceptions','/reports','/import','/data-management','/settings','/logs','/carryover-center']);

function hostOnly(value=''){return String(value||'').trim().toLowerCase().replace(/^\[|\]$/g,'').split(':')[0];}
function ipOnly(value=''){return String(value||'').replace(/^::ffff:/,'');}
function privateV4(value=''){return /^10\.|^192\.168\.|^172\.(1[6-9]|2\d|3[01])\./.test(String(value||''));}
function localChannel(req){const host=hostOnly(req.hostname||req.get?.('host')||''),ip=ipOnly(req.socket?.remoteAddress||'');if(['127.0.0.1','localhost','::1'].includes(host)&&(ip==='127.0.0.1'||ip==='::1'))return 'LOCAL';if(net.isIP(host)===4&&privateV4(host)&&privateV4(ip))return 'LAN';return '';}
function cookieValue(req,name){return String(req.get?.('cookie')||'').split(';').map(v=>v.trim()).find(v=>v.startsWith(`${name}=`))?.slice(name.length+1)||'';}
function sessionSecret(){const env=String(process.env.CE_QC_LOCAL_SESSION_SECRET||'').trim();if(env.length>=32)return env;try{return fs.readFileSync(path.join(getRuntimeConfig().tokenDir,'v213_local_fast_session.secret'),'utf8').trim();}catch{return '';}}
function verifyToken(token,secret=sessionSecret()){try{if(!secret)return null;const [body,sig,extra]=String(token||'').split('.');if(!body||!sig||extra)return null;const expected=crypto.createHmac('sha256',secret).update(body).digest('base64url');const a=Buffer.from(sig),b=Buffer.from(expected);if(a.length!==b.length||!crypto.timingSafeEqual(a,b))return null;const payload=JSON.parse(Buffer.from(body,'base64url').toString('utf8'));if(payload?.v!==213||!payload?.exp||Number(payload.exp)<=Date.now()||!payload?.user?.username||!payload?.user?.role)return null;return payload;}catch{return null;}}
function validFastSession(req){const channel=localChannel(req);const payload=verifyToken(cookieValue(req,FAST_COOKIE));return Boolean(channel&&payload&&payload.channel===channel);}
function wantsHtml(req){return String(req.get?.('accept')||'').includes('text/html');}

function loginPage(){return `<!doctype html><html lang="zh-CN"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>CE质控系统内部登录</title><style>body{margin:0;background:#f3f6fa;color:#17324d;font:15px/1.6 system-ui,'Microsoft YaHei',sans-serif;display:grid;place-items:center;min-height:100vh}.box{width:min(600px,calc(100% - 48px));background:#fff;border:1px solid #dce5ef;border-radius:12px;padding:38px 42px;box-shadow:0 18px 50px #16395b18}h1{font-size:30px;margin:0 0 8px;color:#083b6d}.sub{color:#71849a;margin-bottom:20px}label{display:block;margin:14px 0 6px;font-size:18px}input{box-sizing:border-box;width:100%;padding:14px 15px;border:1px solid #cbd8e5;border-radius:7px;font-size:17px}button{width:100%;margin-top:24px;border:0;border-radius:7px;background:#176fe8;color:#fff;padding:14px;font-size:18px;font-weight:700;cursor:pointer}button:disabled{opacity:.65;cursor:wait}#status{min-height:28px;margin-top:12px;font-weight:600;color:#176fe8}#status.error{color:#b42318}#diag{font-size:12px;color:#71849a;margin-top:7px}</style></head><body><main class="box"><h1>CE质控系统内部登录</h1><div class="sub">账号由5179独立认证；认证成功后直接进入系统，不再等待5177登录接管</div><form id="login"><label>用户名</label><input id="username" autocomplete="username" required><label>密码</label><input id="password" type="password" autocomplete="current-password" required><button id="submit" type="submit">登录</button><div id="status" role="status"></div><div id="diag">认证通道：5179 独立认证 Cookie → 5177 系统页面</div></form></main><script>(function(){'use strict';const f=document.getElementById('login'),b=document.getElementById('submit'),s=document.getElementById('status'),d=document.getElementById('diag');const msg=(t,e=false)=>{s.textContent=t||'';s.classList.toggle('error',e)};const host=location.hostname.includes(':')?'['+location.hostname.replace(/^\[|\]$/g,'')+']':location.hostname;const sidecar=location.protocol+'//'+host+':${AUTH_PORT}';async function parse(r){const t=await r.text();try{return t?JSON.parse(t):{}}catch{return {error:t||('HTTP '+r.status)}}}async function timed(url,options,ms){const c=new AbortController(),t=setTimeout(()=>c.abort(),ms);try{return await fetch(url,{...options,signal:c.signal})}finally{clearTimeout(t)}}async function directLogin(body){msg('正在连接5179独立认证服务…');try{const ping=await timed(sidecar+'/api/v213/auth-ping',{credentials:'include',cache:'no-store',headers:{accept:'application/json'}},2200);const pp=await parse(ping);if(!ping.ok||!pp.ok)throw new Error('认证服务未就绪 HTTP '+ping.status);if(d)d.textContent='认证通道：'+(pp.authPathVersion||pp.version||'5179')+' · DB '+(pp.dbReady===false?'未就绪':'就绪');}catch(err){if(err?.name==='AbortError'||err instanceof TypeError)return null;throw err;}msg('正在校验账号…');try{const r=await timed(sidecar+'/api/v213/local-auth/login',{method:'POST',credentials:'include',cache:'no-store',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify(body)},8000);const p=await parse(r);if(!r.ok||p.ok===false){const e=new Error((p.error||p.message||('登录失败 HTTP '+r.status))+(p.code?' ['+p.code+']':''));e.credentialFailure=[400,401,403,423].includes(r.status);throw e;}if(!p.handoffToken)throw new Error('认证服务未返回会话凭证。');return p;}catch(err){if(err.credentialFailure)throw err;if(err?.name==='AbortError'||err instanceof TypeError)return null;throw err;}}async function proxyFallback(body){msg('5179直连暂不可用，正在使用同源备用认证…');const r=await timed('/api/v246/internal-auth/login',{method:'POST',credentials:'same-origin',cache:'no-store',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify(body)},10000);const p=await parse(r);if(!r.ok||p.ok===false)throw new Error((p.error||p.message||('登录失败 HTTP '+r.status))+(p.code?' ['+p.code+']':''));const h=await timed('/api/v223/fast-auth/accept',{method:'POST',credentials:'same-origin',cache:'no-store',headers:{'content-type':'application/json','accept':'application/json'},body:JSON.stringify({handoffToken:p.handoffToken})},5000);const hp=await parse(h);if(!h.ok||hp.ok===false)throw new Error(hp.error||hp.message||'备用会话建立失败');return p;}f.addEventListener('submit',async e=>{e.preventDefault();if(b.disabled)return;b.disabled=true;b.textContent='正在校验账号…';msg('正在启动独立认证…');const body={username:document.getElementById('username').value.trim(),password:document.getElementById('password').value};try{const direct=await directLogin(body);if(direct){b.textContent='登录成功';msg('账号已验证，浏览器会话已建立，正在进入系统…');setTimeout(()=>location.replace('/?v251='+Date.now()),40);return;}await proxyFallback(body);b.textContent='登录成功';msg('备用认证完成，正在进入系统…');setTimeout(()=>location.replace('/?v251fallback='+Date.now()),40);}catch(err){msg(String(err?.message||err),true);b.disabled=false;b.textContent='登录';}});setTimeout(async()=>{try{const r=await timed(sidecar+'/api/v213/auth-ping',{credentials:'include',cache:'no-store',headers:{accept:'application/json'}},1800);const p=await parse(r);if(r.ok&&p.ok&&d)d.textContent='5179认证服务已就绪 · '+(p.authPathVersion||p.version||'')+' · DB '+(p.dbReady===false?'未就绪':'就绪');}catch{}},80);})();</script></body></html>`;}

function v251LoginMiddleware(req,res,next){if(req.method!=='GET'||!APP_PATHS.has(req.path)||!localChannel(req)||!wantsHtml(req)||validFastSession(req))return next();res.setHeader('Cache-Control','no-store, no-cache, must-revalidate');res.setHeader('X-CE-QC-Login-Reliability',V251_COOKIE_FIRST_LOGIN_VERSION);return res.status(200).type('html').send(loginPage());}

if(!express.application[INSTALLED]){Object.defineProperty(express.application,INSTALLED,{value:true});const previousUse=express.application.use;let mounted=false;express.application.use=function v251CookieFirstUse(...args){const functions=args.flat().filter(value=>typeof value==='function');const access=functions.some(fn=>fn.name==='accessIdentity'||fn.name==='v209AccessIdentityNoHang');if(access&&!mounted){mounted=true;previousUse.call(this,v251LoginMiddleware);}return previousUse.apply(this,args);};}

console.log('[CE-QC][V251] cookie-first login armed before V249/V246/access: successful 5179 authentication navigates directly with the signed host cookie; 5177 handoff is fallback-only.');
export const __test={hostOnly,ipOnly,privateV4,localChannel,verifyToken,validFastSession,loginPage};
