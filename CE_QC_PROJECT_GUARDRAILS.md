# CE CCSL金边仓 质控追踪研发 — 项目总底线

本文件是 CE QC 项目的长期开发约束。后续 QC开发16、17、18……以及任何新功能、指标、口径、导入、导出、刷新、数据库、UI、部署改动，均必须先继承并核对 QC开发1–15 已确认规则，不得只按当前一条需求局部修改。

## 1. 历史上下文优先

- 开始任何修改前，先回顾 QC开发1–15 已确认的业务规则、数据口径、UI规则、数据库持久化规则、异常规则、导入导出规则、自动刷新规则、部署/多用户规则和已经修复过的问题。
- 已确认的历史口径不得因为新功能被静默覆盖。
- 若新需求与旧规则冲突，必须先指出冲突与影响范围，再决定迁移方案，不能直接改。

## 2. 新需求必须同时考虑历史数据

任何新增或修改都不能只对“以后新上传的数据”生效。必须先判断已有历史数据是否需要：

- 重算；
- 回填；
- 迁移；
- 重新分类；
- 重建趋势/缓存；
- 重建明细/导出；
- 保持旧口径并做版本隔离。

若历史数据需要变化，必须设计安全迁移和验证，禁止直接覆盖生产历史。

## 3. 一处指标变化必须全链路同步

任何指标、状态、分类或业务规则变化，都必须逐项检查：

- 日报解析/分类；
- 历史日报成员；
- shipment lifecycle / tracking ledger；
- 当前状态；
- 跨日遗留；
- 首页总看板；
- 七业务看板；
- 日/周/月趋势；
- 金边/外省/门店维度；
- 点击数字后的明细；
- SHOPEE 1/2/3派、退回、平均签收天数；
- 报表导出；
- 后台自动刷新；
- 数据库持久化；
- 缓存；
- 恢复/备份；
- 升级门禁与回归测试。

禁止只修页面数字、不修明细/趋势/导出；也禁止只改新数据、不处理历史数据。

## 4. 数据安全底线

- 生产数据库优先级最高；任何升级不能破坏、重置或静默重写历史数据。
- 历史修复必须区分 historical-only 与 current/carry state，避免旧日期回写覆盖最新状态。
- 已签收/已退回等终态事实必须锁定，除非有更强的真实证据纠正。
- 新规则迁移前必须有可恢复备份或等价可验证恢复点。
- 候选版本必须先过门禁，失败时保留 known-good 版本。
- 禁止为了让测试通过而删除真实安全检查；应修业务逻辑或升级过期测试口径。

## 5. 七业务隔离底线

七个物理业务板块必须保持隔离：

- CE
- CEAF
- TBKH
- ALI1688
- SHOPEECN
- SHOPEEVN
- WHPP本土

聚合看板只能显式聚合，不能因为新功能造成跨业务串票、重复计数或历史分类污染。

## 6. 指标分母与证据底线

- 每日指标分母必须以“当天最新有效日报成员”为准，不能用 firstReportDate 代替当天日报成员。
- 状态事实优先使用已验证 lifecycle / tracking ledger 证据，旧 final rows 只允许做明确的缺失回退。
- 仅有空 OPEN 占位账本不等于已分析。
- coverage 未完成时，不得把局部数据冒充完整百分比。
- 无分母的百分比显示为 — / null，不得伪造 0%。

## 7. 用户操作底线

- 能由代码、日志、数据库、归档证据自行定位的问题，不让用户反复上传、点击、试错。
- 在要求用户重启/操作前，先完成能完成的源码检查和回归门禁。
- 一次操作应尽量只要求一次重启或一次明确动作。

## 8. UI与品牌规则继续继承

- 蓝白、简约、Microsoft YaHei。
- 左上角 CE EXPRESS 品牌区永久保留，位置/比例/大小/文字/布局不得随意更改。
- 金边=蓝、外省=橙。
- 趋势必须显示真实百分比；数字点击跳转对应明细。

## 9. 新功能执行顺序

每次接到新需求，默认执行：

1. 先读历史规则与当前数据库/代码现状；
2. 列出对历史数据和全链路的影响；
3. 设计兼容/迁移方案；
4. 先加或更新回归门禁；
5. 修改代码；
6. 验证旧数据不退步、新数据能正确生效；
7. 再允许生产升级。

任何“以后增加什么就直接加上去”的做法均不符合本项目规则。
