const BUSINESSES=['CE','CEAF','TBKH','ALI1688','SHOPEECN','SHOPEEVN','WHPP'];
const LABEL={CE:'CE',CEAF:'CEAF空运',TBKH:'TBKH',ALI1688:'ALI1688',SHOPEECN:'SHOPEE CN',SHOPEEVN:'SHOPEE VN',WHPP:'WHPP'};
let session=null,currentDate='',currentPage='home',currentBoard='';
const $=id=>document.getElementById(id),page=$('page');
const fmt=n=>Number(n||0).toLocaleString('en-US');
const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
async function api(url,options={}){const r=await fetch(url,{cache:'no-store',...options});const text=await r.text();let p={};try{p=text?JSON.parse(text):{};}catch{p={error:text}}if(r.status===401){location.replace('/login');throw new Error('登录已过期');}if(!r.ok||p.ok===false)throw new Error(p.error||`HTTP ${r.status}`);return p;}

async function boot(){
  const status=await api('/api/system/status');session=status.user;currentDate=status.latestReportDate||'';
  $('userName').textContent=session.displayName||session.username;$('userRole').textContent=`${session.department||'质控部'} · ${session.role}`;
  if(currentDate)$('dateSelect').value=currentDate;
  bindNav();route(location.pathname);window.addEventListener('popstate',()=>route(location.pathname));
}
function bindNav(){document.querySelectorAll('#nav button').forEach(btn=>btn.addEventListener('click',()=>{if(btn.dataset.board)go(`/board/${btn.dataset.board.toLowerCase()}`);else go(btn.dataset.page==='home'?'/':`/${btn.dataset.page}`)}));$('dateQuery').addEventListener('click',()=>{currentDate=$('dateSelect').value||currentDate;renderCurrent();});}
function go(path){history.pushState({},'',path);route(path);}
function route(path){const m=path.match(/^\/board\/([a-z0-9]+)$/i);if(m){currentBoard=m[1].toUpperCase();currentPage='board';}else{currentBoard='';currentPage=path==='/'?'home':path.slice(1)||'home';}renderCurrent();}
function setActive(){document.querySelectorAll('#nav button').forEach(btn=>btn.classList.toggle('active',currentPage==='board'?btn.dataset.board===currentBoard:btn.dataset.page===currentPage));}
async function renderCurrent(){setActive();page.innerHTML='<div class="panel"><div class="empty">正在读取…</div></div>';try{if(currentPage==='home')return renderHome();if(currentPage==='board')return renderBoard(currentBoard);if(currentPage==='import')return renderImport();if(currentPage==='settings')return renderSettings();if(currentPage==='tracking')return renderTracking();if(currentPage==='exceptions')return renderExceptions();if(currentPage==='reports')return renderReports();return renderHome();}catch(error){page.innerHTML=`<div class="panel"><div class="empty" style="color:#dc3f58">${esc(error.message)}</div></div>`;}}

async function renderHome(){
  $('pageTitle').textContent='首页总看板';const result=await api(`/api/boards${currentDate?`?date=${encodeURIComponent(currentDate)}`:''}`);currentDate=result.reportDate||currentDate;if(currentDate)$('dateSelect').value=currentDate;
  const cards=BUSINESSES.map(type=>{const b=result.boards[type];return `<article class="card ${type==='WHPP'?'whpp':''}" data-open-board="${type}"><strong>${LABEL[type]}看板</strong><div class="number">${fmt(b.total)}</div><small>签收 ${fmt(b.pod)} · 未闭环 ${fmt(b.open)} · POD率 ${Number(b.podRate||0).toFixed(2)}%</small></article>`}).join('');
  const totals=BUSINESSES.reduce((a,t)=>{const b=result.boards[t];a.total+=b.total;a.pod+=b.pod;a.open+=b.open;a.returned+=b.returned;return a;},{total:0,pod:0,open:0,returned:0});
  page.innerHTML=`<div class="pagehead"><div><h2>首页总看板</h2><p>${result.hasData?`日报 ${esc(result.reportDate)} · 七业务严格分开`:'当前是全新数据池，尚未导入日报'}</p></div><span class="tag ${result.hasData?'ok':''}">${result.hasData?'已加载新数据':'空数据可正常使用'}</span></div><div class="grid boards">${cards}</div><section class="panel"><div class="panel-title">全业务汇总</div><div class="panel-body"><div class="grid metrics"><div class="metric"><span>总票数</span><b>${fmt(totals.total)}</b></div><div class="metric"><span>已POD</span><b>${fmt(totals.pod)}</b></div><div class="metric"><span>已退回</span><b>${fmt(totals.returned)}</b></div><div class="metric"><span>未闭环</span><b>${fmt(totals.open)}</b></div><div class="metric"><span>七业务</span><b>7</b></div><div class="metric"><span>WHPP</span><b>${fmt(result.boards.WHPP.total)}</b></div></div></div></section>`;
  document.querySelectorAll('[data-open-board]').forEach(el=>el.addEventListener('click',()=>go(`/board/${el.dataset.openBoard.toLowerCase()}`)));
}

async function renderBoard(type){
  if(!BUSINESSES.includes(type))return go('/');$('pageTitle').textContent=`${LABEL[type]}看板`;
  const result=await api(`/api/boards/${type}${currentDate?`?date=${encodeURIComponent(currentDate)}`:''}`),b=result.board;currentDate=b.reportDate||currentDate;if(currentDate)$('dateSelect').value=currentDate;
  const metrics=[['今日票数',b.total],['签收件数',b.pod],['已退回件',b.returned],['当前未闭环',b.open],['POD率',`${Number(b.podRate||0).toFixed(2)}%`],['Pending1+',b.pending1],['Pending2+',b.pending2],['Pending3+',b.pending3],['OC1+',b.oc1],['OC2+',b.oc2],['OC3+',b.oc3],['普通未闭环',b.unresolved],['仓库自提',b.selfPickup],['CECN',b.cecn],['CEZT',b.cezt],['580滞留',b.retention580],['金边PP',b.pp],['外省PV',b.pv]];
  page.innerHTML=`<div class="pagehead"><div><h2>${LABEL[type]}看板</h2><p>${b.reportDate?`日报 ${esc(b.reportDate)}`:'暂无日报'}${type==='WHPP'?' · WHPP为独立一等业务板块':''}</p></div><button class="btn ghost" id="refreshBoard">刷新</button></div><div class="grid metrics">${metrics.map(([k,v])=>`<div class="metric"><span>${esc(k)}</span><b>${typeof v==='string'?esc(v):fmt(v)}</b></div>`).join('')}</div><section class="panel"><div class="panel-title"><div class="toolbar"><b>运单明细</b><input id="rowSearch" placeholder="搜索运单号/收件人"><select id="rowRegion"><option value="">全部区域</option><option value="PP">金边PP</option><option value="PV">外省PV</option></select><button class="btn ghost" id="rowQuery">查询</button></div></div><div id="rowTable" class="table-wrap"></div></section>`;
  $('refreshBoard').onclick=()=>renderBoard(type);$('rowQuery').onclick=()=>loadRows(type);await loadRows(type);
}
async function loadRows(type){const q=$('rowSearch')?.value||'',region=$('rowRegion')?.value||'';const p=await api(`/api/boards/${type}/rows?date=${encodeURIComponent(currentDate||'')}&region=${encodeURIComponent(region)}&q=${encodeURIComponent(q)}&limit=500`);$('rowTable').innerHTML=p.rows.length?`<table><thead><tr><th>运单号</th><th>区域</th><th>状态</th><th>Pending天数</th><th>OC天数</th><th>特殊状态</th><th>最后节点</th></tr></thead><tbody>${p.rows.map(r=>`<tr><td>${esc(r.shipmentCode)}</td><td>${esc(r.regionCode||'—')}</td><td>${esc(r.state||'UNPROCESSED')}</td><td>${fmt(r.pendingDays)}</td><td>${fmt(r.ocDays)}</td><td>${esc(r.specialState||'—')}</td><td>${esc(r.lastEventDesc||'—')}</td></tr>`).join('')}</tbody></table>`:'<div class="empty">当前没有明细</div>';}

function renderImport(){
  $('pageTitle').textContent='数据导入';page.innerHTML=`<div class="pagehead"><div><h2>综合日报导入</h2><p>新数据池 · 一次上传，严格分类到7个板块，包括WHPP</p></div></div><div class="grid split"><section class="panel"><div class="panel-title">上传日报Excel</div><div class="panel-body"><div class="notice">旧22GB业务数据库不会被写入。QC Next使用全新的业务数据数据库。</div><form id="importForm"><div class="field"><label>日报日期（可留空自动识别）</label><input id="importDate" type="date"></div><div class="field" style="margin-top:12px"><label>综合日报Excel</label><input id="importFile" type="file" accept=".xlsx,.xls" required></div><button class="btn primary" style="margin-top:14px" type="submit">导入并分类7业务</button><div id="importStatus" class="status"></div></form></div></section><section class="panel"><div class="panel-title">分类结果</div><div id="importResult" class="panel-body"><div class="empty">导入后显示7业务票数与源数据守恒结果</div></div></section></div>`;
  $('importForm').onsubmit=async e=>{e.preventDefault();const file=$('importFile').files[0];if(!file)return;const fd=new FormData();fd.append('file',file);if($('importDate').value)fd.append('reportDate',$('importDate').value);const st=$('importStatus');st.className='status';st.textContent='正在解析并严格分类…';try{const r=await api('/api/import/daily',{method:'POST',body:fd});st.className='status ok';st.textContent=`导入成功：${r.reportDate} · ${fmt(r.total)}票`;currentDate=r.reportDate;$('dateSelect').value=currentDate;$('importResult').innerHTML=`<div class="grid metrics">${BUSINESSES.map(t=>`<div class="metric"><span>${LABEL[t]}</span><b>${fmt(r.classificationCounts[t])}</b></div>`).join('')}</div><div class="notice">7业务合计 ${fmt(r.total)} 票；分类守恒通过。WHPP ${fmt(r.classificationCounts.WHPP)} 票。</div>`;}catch(err){st.className='status error';st.textContent=err.message;}};
}

async function renderSettings(){
  $('pageTitle').textContent='系统设置';const [status,ce]=await Promise.all([api('/api/system/status'),api('/api/ce/status')]);
  page.innerHTML=`<div class="pagehead"><div><h2>系统设置</h2><p>系统账户、CE API、数据清理完全独立于看板加载</p></div><span class="tag ok">QC NEXT CLEAN_REBUILD</span></div><div class="grid split"><section class="panel"><div class="panel-title">当前系统账户</div><div class="panel-body"><p><b>${esc(status.user.displayName||status.user.username)}</b> · ${esc(status.user.role)}</p><p>用户名：${esc(status.user.username)}</p><p>最新日报：${esc(status.latestReportDate||'暂无')}</p><button class="btn ghost" id="logout">退出登录</button></div></section><section class="panel"><div class="panel-title">CE API账号</div><div class="panel-body"><div class="notice">${ce.loggedIn?`已登录：${esc(ce.account)}`:'尚未登录CE API；这不会影响QC系统内部登录。'}</div><div class="form-grid"><div class="field"><label>tenantId</label><input id="ceTenant" value="000000"></div><div class="field"><label>CE username</label><input id="ceUser"></div><div class="field"><label>CE password</label><input id="cePass" type="password"></div></div><button class="btn primary" id="ceLogin" style="margin-top:12px">登录CE系统</button><div id="ceStatus" class="status"></div></div></section></div>${status.user.role==='ADMIN'?`<section class="panel" style="margin-top:14px"><div class="panel-title">新业务数据池</div><div class="panel-body"><p>只清空QC Next业务数据，不删除内部账号/权限/设置，也不动旧22GB数据库。</p><button class="btn danger" id="clearData">永久清除全部业务数据</button><div id="clearStatus" class="status"></div></div></section>`:''}`;
  $('logout').onclick=async()=>{await api('/api/auth/logout',{method:'POST'});location.replace('/login');};
  $('ceLogin').onclick=async()=>{const st=$('ceStatus');st.className='status';st.textContent='正在连接CE API…';try{const r=await api('/api/ce/login',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({tenantId:$('ceTenant').value,username:$('ceUser').value.trim(),password:$('cePass').value})});st.className='status ok';st.textContent=`CE API登录成功：${r.account}`;}catch(err){st.className='status error';st.textContent=err.message;}};
  if($('clearData'))$('clearData').onclick=async()=>{if(!confirm('确定清空QC Next全部业务数据？系统账号和旧数据库不会删除。'))return;const phrase=prompt('请输入：永久清除全部业务数据');if(phrase!=='永久清除全部业务数据')return;try{await api('/api/admin/clear-business-data',{method:'POST',headers:{'content-type':'application/json'},body:JSON.stringify({confirm:phrase})});$('clearStatus').className='status ok';$('clearStatus').textContent='QC Next业务数据已清空。';currentDate='';$('dateSelect').value='';}catch(err){$('clearStatus').className='status error';$('clearStatus').textContent=err.message;}};
}

function renderTracking(){$('pageTitle').textContent='轨迹查询';page.innerHTML='<div class="pagehead"><div><h2>轨迹查询</h2><p>QC Next 的扫描/轨迹处理模块将直接写入新数据池，不再穿过旧版patch链。</p></div></div><section class="panel"><div class="empty">当前重构阶段：等待新状态引擎接入。</div></section>';}
function renderExceptions(){$('pageTitle').textContent='异常明细';page.innerHTML='<div class="pagehead"><div><h2>异常明细</h2><p>Pending / OC / 门店 / 580 / 自提 / 退回将由新状态引擎统一计算。</p></div></div><section class="panel"><div class="empty">当前重构阶段：等待新状态引擎接入。</div></section>';}
function renderReports(){$('pageTitle').textContent='报表导出';page.innerHTML='<div class="pagehead"><div><h2>报表导出</h2><p>新导出器将直接从QC Next数据池生成7业务工作簿。</p></div></div><section class="panel"><div class="empty">当前重构阶段：等待新导出器接入。</div></section>';}

boot().catch(error=>{page.innerHTML=`<div class="panel"><div class="empty" style="color:#dc3f58">系统启动失败：${esc(error.message)}</div></div>`;});
