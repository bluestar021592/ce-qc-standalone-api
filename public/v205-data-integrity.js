(function v205DataIntegrity(global){
  'use strict';
  if(global.__CE_QC_V205_DATA_INTEGRITY__)return;
  global.__CE_QC_V205_DATA_INTEGRITY__='2026-08-18-v205-visible-completeness-audit-v1';
  const TYPES=['CE','CEAF','TBKH','ALI1688','SHOPEECN','SHOPEEVN','WHPP'];
  const LABELS={CE:'CE',CEAF:'CEAF空运',TBKH:'TBKH',ALI1688:'ALI1688',SHOPEECN:'SHOPEE CN',SHOPEEVN:'SHOPEE VN',WHPP:'WHPP本土'};
  const q=(s,r=document)=>r.querySelector(s);const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));
  function dateKey(d){return new Date(d).toISOString().slice(0,10);}
  function defaultRange(){const end=new Date(),start=new Date(end.getTime()-60*86400000);return{from:dateKey(start),to:dateKey(end)};}
  async function api(url){const res=await fetch(url,{credentials:'same-origin',cache:'no-store'});let data={};try{data=await res.json();}catch{}if(!res.ok||data.ok===false)throw new Error(data.error||`HTTP ${res.status}`);return data;}
  function style(){if(q('#v205IntegrityStyle'))return;const s=document.createElement('style');s.id='v205IntegrityStyle';s.textContent=`
  .v205-integrity{margin:16px 0}.v205-integrity-head{display:flex;justify-content:space-between;gap:12px;align-items:flex-start}.v205-integrity-head h3{margin:0;color:#103f6f}.v205-integrity-head p{margin:5px 0 0;color:#748aa3;font-size:13px}.v205-controls{display:grid;grid-template-columns:1fr 1fr 1fr auto;gap:10px;margin:12px 0}.v205-controls label{font-size:12px;color:#59738f}.v205-controls input,.v205-controls select{width:100%;height:38px;border:1px solid #cfdceb;border-radius:6px;padding:0 9px;background:#fff}.v205-grid{display:grid;grid-template-columns:repeat(3,minmax(190px,1fr));gap:10px}.v205-card{border:1px solid #dbe7f4;border-radius:9px;padding:11px;background:#fff}.v205-card h4{margin:0 0 8px;color:#184b7d}.v205-kpis{display:grid;grid-template-columns:repeat(3,1fr);gap:7px}.v205-kpis small{display:block;color:#7b8fa5}.v205-kpis b{display:block;margin-top:3px;font-size:20px;color:#145d9b}.v205-danger{color:#c43b34!important}.v205-good{color:#25834f!important}.v205-note{margin-top:10px;padding:9px 11px;background:#f4f8fd;border-radius:7px;color:#5c748e;font-size:12px;line-height:1.55}.v205-deep{margin-top:12px;border-top:1px solid #e3ebf4;padding-top:12px}.v205-samples{max-height:220px;overflow:auto;margin-top:8px}.v205-samples table{width:100%;border-collapse:collapse;min-width:760px}.v205-samples th,.v205-samples td{padding:6px 8px;border-bottom:1px solid #e7eef6;text-align:left;font-size:12px}.v205-error{color:#b33b35}.v205-loading{color:#6c8198}@media(max-width:1200px){.v205-grid{grid-template-columns:1fr 1fr}.v205-controls{grid-template-columns:1fr 1fr}}`;
  document.head.appendChild(s);}
  function host(){return q('#importPage')||q('#dataManagementPage')||q('#data-management')||null;}
  function ensure(){const root=host();if(!root)return null;let panel=q('#v205IntegrityPanel');if(panel&&panel.closest('.app-page')!==root){panel.remove();panel=null;}if(panel)return panel;const range=defaultRange();panel=document.createElement('section');panel.id='v205IntegrityPanel';panel.className='panel v205-integrity';panel.innerHTML=`<div class="v205-integrity-head"><div><h3>数据完整性 / 遗漏风险核查</h3><p>不是抽查几个单号：按历史日报底账、当前快照、扫描/轨迹证据三层自动对账</p></div><span class="status-pill">V205</span></div><div class="v205-controls"><label>开始日期<input id="v205IntegrityFrom" type="date" value="${range.from}"></label><label>结束日期<input id="v205IntegrityTo" type="date" value="${range.to}"></label><label>深度核查业务<select id="v205IntegrityType">${TYPES.map(t=>`<option value="${t}">${LABELS[t]}</option>`).join('')}</select></label><button id="v205IntegrityRun" class="btn primary" type="button">开始完整性核查</button></div><div id="v205IntegrityBody" class="v205-loading">等待核查…</div><div id="v205IntegrityDeep" class="v205-deep"></div>`;root.appendChild(panel);q('#v205IntegrityRun',panel).onclick=run;return panel;}
  async function run(){const panel=ensure();if(!panel)return;const from=q('#v205IntegrityFrom',panel).value,to=q('#v205IntegrityTo',panel).value,type=q('#v205IntegrityType',panel).value,body=q('#v205IntegrityBody',panel),deep=q('#v205IntegrityDeep',panel);body.innerHTML='正在核对所有已完成日报快照，检查是否有被后续上传覆盖而消失的运单…';deep.innerHTML='';try{const summary=await api(`/api/v205/integrity/summary?fromDate=${encodeURIComponent(from)}&toDate=${encodeURIComponent(to)}`);body.innerHTML=`<div class="v205-grid">${(summary.byBusiness||[]).map(x=>`<div class="v205-card"><h4>${esc(LABELS[x.businessType]||x.businessType)}</h4><div class="v205-kpis"><div><small>历史底账</small><b>${Number(x.archiveUnique||0).toLocaleString('zh-CN')}</b></div><div><small>当前快照</small><b>${Number(x.currentUnique||0).toLocaleString('zh-CN')}</b></div><div><small>遗漏风险</small><b class="${x.atRiskMissing?'v205-danger':'v205-good'}">${Number(x.atRiskMissing||0).toLocaleString('zh-CN')}</b></div></div><small>${x.datesAtRisk?`${x.datesAtRisk} 个日期存在当前快照少票`:'当前未发现快照覆盖遗漏'}</small></div>`).join('')}</div><div class="v205-note"><b>底账原则：</b>${esc(summary.rule||'')}<br>“遗漏风险”不是猜测，而是历史已完成日报里存在、但当前同日快照里已经消失的运单数量。V205导出仍会从历史底账并集恢复这些票，不再静默丢失。</div>`;await runDeep(type,from,to,deep);}catch(error){body.innerHTML=`<div class="v205-error">完整性核查失败：${esc(error.message)}</div>`;}}
  async function runDeep(type,from,to,root){root.innerHTML=`<div class="v205-loading">正在深度检查 ${esc(LABELS[type]||type)} 的轨迹覆盖…</div>`;try{const data=await api(`/api/v205/integrity/deep?businessType=${encodeURIComponent(type)}&fromDate=${encodeURIComponent(from)}&toDate=${encodeURIComponent(to)}`);root.innerHTML=`<div class="v205-grid"><div class="v205-card"><h4>${esc(LABELS[type]||type)} 深度核查</h4><div class="v205-kpis"><div><small>底账票数</small><b>${Number(data.total||0).toLocaleString('zh-CN')}</b></div><div><small>已有扫描/轨迹证据</small><b class="v205-good">${Number(data.withEvidence||0).toLocaleString('zh-CN')}</b></div><div><small>证据待补齐</small><b class="${data.dataIntegrityReview?'v205-danger':'v205-good'}">${Number(data.dataIntegrityReview||0).toLocaleString('zh-CN')}</b></div></div><small>证据覆盖率 ${Number(data.coverageRate||0).toFixed(2)}%</small></div></div>${(data.reviewSamples||[]).length?`<div class="v205-samples"><table><thead><tr><th>运单号</th><th>首次日报</th><th>日报状态</th><th>当前说明</th><th>最后节点时间</th><th>最后节点</th></tr></thead><tbody>${data.reviewSamples.map(r=>`<tr><td>${esc(r.shipmentCode)}</td><td>${esc(r.date)}</td><td>${esc(r.statusCode)}</td><td>${esc(r.statusDesc)}</td><td>${esc(r.latestEventTime||'—')}</td><td>${esc(r.latestEventDesc||'证据待补齐')}</td></tr>`).join('')}</tbody></table></div>`:'<div class="v205-note"><b>结果：</b>所选业务没有发现“日报有票但扫描/轨迹证据缺失”的记录。</div>'}`;}catch(error){root.innerHTML=`<div class="v205-error">深度核查失败：${esc(error.message)}</div>`;}}
  function tick(){style();ensure();}
  function boot(){tick();const mo=new MutationObserver(()=>{clearTimeout(global.__v205IntegrityTick);global.__v205IntegrityTick=setTimeout(tick,150);});mo.observe(document.documentElement,{childList:true,subtree:true,attributes:true,attributeFilter:['hidden','class']});}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',boot,{once:true});else boot();
})(window);
