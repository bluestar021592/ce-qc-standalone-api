(function installV142HistoryAudit(global){
  if(global.__CE_QC_V142_HISTORY_AUDIT__)return;global.__CE_QC_V142_HISTORY_AUDIT__=true;
  const VERSION='2026-08-30-v375-history-audit-open-carry-clarity-v1';
  const esc=v=>String(v??'').replace(/[&<>"']/g,ch=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[ch]));
  const fmt=v=>Number(v||0).toLocaleString('zh-CN');
  function visible(){return location.pathname==='/import'||document.getElementById('importPage')?.classList?.contains('active');}
  function host(){const page=document.getElementById('importPage');if(!page)return null;let panel=document.getElementById('v142HistoryAudit');if(panel)return panel;panel=document.createElement('section');panel.id='v142HistoryAudit';panel.className='panel operation-panel';panel.innerHTML=`<div class="panel-title"><div><h3>历史数据完整性 / 导出安全检查</h3><p>只读检查SQLite，不修改、不删除任何历史数据。</p></div><span class="status-pill muted">7业务</span></div><div id="v142AuditBody" class="operation-status">正在检查7月1日至当前最新日报…</div><div class="button-row" style="margin-top:10px"><button id="v142AuditRefresh" class="btn ghost compact" type="button">重新检查</button></div>`;page.appendChild(panel);panel.querySelector('#v142AuditRefresh')?.addEventListener('click',load);return panel;}
  async function load(){if(!visible())return;const panel=host();if(!panel)return;const body=panel.querySelector('#v142AuditBody');body.innerHTML='<span class="status-pill warning">正在只读核对历史日报、快照、跨日持续追踪和7业务导出覆盖…</span>';try{const r=await fetch('/api/v142/history-integrity?fromDate=2026-07-01',{cache:'no-store',credentials:'same-origin'});const p=await r.json();if(!r.ok||p.ok===false)throw new Error(p.error||`HTTP ${r.status}`);const missing=(p.missingDates||[]);const incomplete=(p.incompleteDates||[]);const open=p.currentEvidence?.carryOpen||0;const cls=p.currentEvidence?.carryClosed||0;const latestProcessingIssues=new Set(['CORE_SNAPSHOT_NOT_COMPLETED','WHPP_SNAPSHOT_MISSING','WHPP_FINAL_ROWS_INCOMPLETE']);const latestDayPending=missing.length===0&&incomplete.length>0&&incomplete.every(x=>String(x.reportDate||'')===String(p.toDate||'')&&(x.issues||[]).length>0&&(x.issues||[]).every(issue=>latestProcessingIssues.has(String(issue||''))));const clsName=p.exportReady?'success':(latestDayPending?'warning':'danger');const title=p.exportReady?(p.totalRetryPending>0?'历史覆盖完整，可导出（仍有接口待重试）':'历史覆盖完整，可安全导出'):(latestDayPending?'当前最新日报尚未处理完成，区间导出暂不可用':'历史完整性未通过，已禁止静默缺数据导出');body.innerHTML=`<span class="status-pill ${clsName}">${esc(title)}</span><p><b>${esc(p.fromDate)} 至 ${esc(p.toDate)}</b> · 应有 ${fmt(p.expectedDays)} 天 · 已找到 ${fmt(p.daysPresent)} 天 · 总导入 ${fmt(p.totalImported)} 票</p><p>跨日仍在持续追踪 <b>${fmt(open)}</b> · 历史已闭环 <b>${fmt(cls)}</b> · WHPP待重试 <b>${fmt(p.totalRetryPending)}</b></p>${open?'<p class="muted">“持续追踪”来自所选历史区间 carryover OPEN，不是本次日报突然新增的票；后续扫描/轨迹/POD会继续关闭这些历史单。</p>':''}${missing.length?`<p class="danger-text">缺少日期：${missing.map(esc).join('、')}</p>`:''}${incomplete.length?`<p class="${latestDayPending?'muted':'danger-text'}">待核对日期：${incomplete.slice(0,8).map(x=>`${esc(x.reportDate)}（${esc((x.issues||[]).join('/'))}）`).join('；')}${incomplete.length>8?'…':''}</p>`:''}<p class="muted">导出执行严格7业务覆盖检查：CE、CEAF、TBKH、ALI1688、SHOPEE CN、SHOPEE VN、WHPP。当前最新日报未处理完成时只暂缓导出，不代表历史数据被删除或本次导入多出了异常票。</p>`;}catch(error){body.innerHTML=`<span class="status-pill danger">历史完整性检查失败：${esc(error.message)}</span>`;}}
  function ensure(){if(!visible())return;host();load();}
  document.addEventListener('click',e=>{if(e.target?.closest?.('[data-page="import"],.side-link[data-path="/import"]'))setTimeout(ensure,150);},true);
  document.addEventListener('ce-qc-run-complete',()=>setTimeout(load,300));
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',()=>setTimeout(ensure,200),{once:true});else setTimeout(ensure,200);
  global.__CE_QC_V142_HISTORY_AUDIT__={version:VERSION,refresh:load};console.info('[CE-QC][V142_HISTORY_AUDIT]',VERSION);
})(window);