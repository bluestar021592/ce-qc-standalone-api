(function installV329DashboardReadBridge(global){
  if(global.__CE_QC_V308_DASHBOARD_READ_BRIDGE__)return;
  const VERSION='2026-08-27-v329-three-business-cache-ui-v1',SPECIAL=new Set(['TBKH','SHOPEECN','SHOPEEVN']);
  const nativeFetch=global.fetch.bind(global),inFlight=new Map();let tableTimer=null,lastTableKey='',evidenceTimer=null,evidenceWatchKey='',evidencePolls=0,evidenceCacheVersion=0;
  const date=v=>String(v||'').slice(0,10),num=v=>Number.isFinite(Number(v))?Number(v):0,fmt=v=>Number.isFinite(Number(v))?Number(v).toLocaleString('zh-CN'):'—',pct=v=>v===null||v===undefined||!Number.isFinite(Number(v))?'—':`${Number(v).toFixed(2)}%`,esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot',"'":'&#39;'}[c]));
  const labelOf=t=>t==='TBKH'?'TBKH':t==='SHOPEECN'?'SHOPEE CN':'SHOPEE VN';
  function installStyle(){if(document.getElementById('v308DashboardStyle'))return;const s=document.createElement('style');s.id='v308DashboardStyle';s.textContent=`.v18-trend-section .v271-trend-status{display:none!important}#v308DeliveryDailyTable{margin-top:14px;overflow:hidden}#v308DeliveryDailyTable .v308-head{display:flex;align-items:flex-start;justify-content:space-between;gap:16px;margin-bottom:10px}#v308DeliveryDailyTable .v308-head h2{margin:0;font-size:18px;color:#123a67}#v308DeliveryDailyTable .v308-head p{margin:4px 0 0;color:#6f849b;font-size:12px;line-height:1.55}#v308DeliveryDailyTable .v308-state{padding:8px 10px;border-radius:7px;background:#eef7ff;color:#315d86;font-size:12px}#v308DeliveryDailyTable .v308-state.warn{background:#fff7e6;color:#8a6200}#v308DeliveryDailyTable .v308-state.error{background:#fff1f1;color:#a61b1b}#v308DeliveryDailyTable .v308-table-wrap{overflow:auto;border:1px solid #dfe9f4;border-radius:9px;background:#fff}#v308DeliveryDailyTable table{width:100%;min-width:1260px;border-collapse:collapse;font-size:12px}#v308DeliveryDailyTable th{position:sticky;top:0;background:#f4f8fc;color:#35546f;font-weight:700;white-space:nowrap}#v308DeliveryDailyTable th,#v308DeliveryDailyTable td{padding:10px 9px;border-right:1px solid #e5edf5;border-bottom:1px solid #e5edf5;text-align:center}#v308DeliveryDailyTable th:last-child,#v308DeliveryDailyTable td:last-child{border-right:0}#v308DeliveryDailyTable tbody tr:last-child td{border-bottom:0}#v308DeliveryDailyTable td:first-child,#v308DeliveryDailyTable th:first-child{text-align:left;padding-left:12px}#v308DeliveryDailyTable .v308-missing{color:#9aaabc}#v308DeliveryDailyTable .v308-foot{margin:8px 2px 0;color:#6f849b;font-size:11px;line-height:1.55}`;document.head.appendChild(s);}
  function parseUrl(raw){try{return new URL(raw,location.origin);}catch{return null;}}function selectedTypeFromUrl(u){return String(u?.searchParams?.get('businessType')||'').toUpperCase();}
  function rewrite(raw){const u=parseUrl(raw);if(!u||u.origin!==location.origin)return raw;const type=selectedTypeFromUrl(u);if((u.pathname==='/api/v263/delivery-trends'||u.pathname==='/api/v253/trends')&&SPECIAL.has(type)){u.pathname='/api/v308/delivery-daily';return u.pathname+u.search;}if(u.pathname==='/api/v273/trends'){u.pathname='/api/v253/trends';return u.pathname+u.search;}return raw;}
  function methodOf(input,init){return String(init?.method||input?.method||'GET').toUpperCase();}function shouldCoalesce(raw){const u=parseUrl(raw);return Boolean(u&&['/api/v308/delivery-daily','/api/v253/trends','/api/v273/trends','/api/v263/delivery-trends','/api/v319/trends'].includes(u.pathname));}
  function fetchShared(url,init={}){const key=String(url);let pending=inFlight.get(key);if(!pending){const controller=new AbortController(),timer=setTimeout(()=>controller.abort(),5000),safeInit={...init,signal:controller.signal,cache:'no-store',credentials:init.credentials||'same-origin'};pending=nativeFetch(url,safeInit).finally(()=>{clearTimeout(timer);setTimeout(()=>inFlight.delete(key),200);});inFlight.set(key,pending);}return pending.then(r=>r.clone());}
  global.fetch=function v329DashboardFetch(input,init){try{if(methodOf(input,init)!=='GET')return nativeFetch(input,init);const raw=typeof input==='string'?input:String(input?.url||''),next=rewrite(raw);if(!shouldCoalesce(next))return nativeFetch(input,init);return fetchShared(next,init||{});}catch{return nativeFetch(input,init);}};
  function activeAttemptType(){const t=String(document.getElementById('pageTitle')?.textContent||'').toUpperCase(),p=String(location.pathname||'').toLowerCase();if(t.includes('TBKH')||p.includes('tbkh'))return'TBKH';if(t.includes('SHOPEE CN')||p.includes('shopeecn'))return'SHOPEECN';if(t.includes('SHOPEE VN')||p.includes('shopeevn'))return'SHOPEEVN';return'';}function visibleRoot(){return[...document.querySelectorAll('.app-page')].find(n=>!n.hidden&&getComputedStyle(n).display!=='none')||null;}function selectedRange(){const to=date(document.getElementById('topRangeTo')?.value||document.getElementById('dashboardRangeTo')?.value||''),from=date(document.getElementById('topRangeFrom')?.value||document.getElementById('dashboardRangeFrom')?.value||to);return{from,to};}
  function cell(v,available=true){return available&&v!==null&&v!==undefined&&Number.isFinite(Number(v))?fmt(v):'<span class="v308-missing">—</span>';}function rateCell(v){return v===null||v===undefined||!Number.isFinite(Number(v))?'<span class="v308-missing">—</span>':pct(v);}function sampleCell(k,t){return t?`${fmt(k)} / ${fmt(t)}`:fmt(k);}
  function repairText(repair,partial){const s=String(repair?.status||'').toUpperCase();if(['QUEUED','STARTING','RUNNING'].includes(s))return`；后台缓存/证据校准中 ${fmt(repair?.completed||0)}/${fmt(repair?.total||0)}`;if(s==='COMPLETED'&&Number(repair?.signingUnresolved||repair?.unresolved||0)>0)return`；仍有 ${fmt(repair.signingUnresolved||repair.unresolved)} 票真实POD日期/派次证据待补`;return partial?'；缺失证据保持未知，不伪造0':'';}
  function renderTable(root,type,data,rg){root.querySelector('#v308DeliveryDailyTable')?.remove();const panel=document.createElement('section');panel.id='v308DeliveryDailyTable';panel.className='v18-panel';const rows=Array.isArray(data?.daily)?data.daily:[],partial=rows.some(r=>r?.evidenceIncomplete),history=Boolean(data?.historyExpanded),actualFrom=data?.fromDate||rg.from,actualTo=data?.toDate||rg.to,repair=data?.evidenceRepair||{},pending=Boolean(data?.historyCachePending);
    panel.innerHTML=`<div class="v308-head"><div><h2>每日派次与签收时效明细</h2><p>${labelOf(type)} · ${history?`自动读取已保存历史日报 ${esc(actualFrom)} → ${esc(actualTo)}；顶部仍保持 ${esc(rg.to)} 当日卡片快速查看。`:'严格按顶部日期读取。'} 平均签收天数=实际POD日期−首次日报锁定日期+1；仅已完成POD参与平均。</p></div><div class="v308-state ${(partial||pending)?'warn':''}">${pending?'历史缓存正在独立后台建立，当前先显示当天数据':rows.length?`已读取 ${rows.length} 个日报日期${repairText(repair,partial)}`:'没有可读取日报'}</div></div>`;
    if(rows.length){const body=rows.map(row=>{const ready=row?.ready!==false,pod=num(row?.pod),known=num(row?.attempt1Known??row?.attempt1)+num(row?.attempt2Known??row?.attempt2)+num(row?.attempt3Known??row?.attempt3),avg=row?.avgSigningDays??row?.avgDispatchSigningDays,attemptAvailable=ready&&(pod===0||known>0||row?.attemptEvidenceComplete===true),signingKnown=num(row.signingSampleCount??row.signingDaysCount);return `<tr><td>${esc(row.reportDate||'')}</td><td>${cell(row.total,true)}</td><td>${cell(row.pod,ready)}</td><td>${ready?rateCell(row.podRate):'<span class="v308-missing">—</span>'}</td><td>${cell(row.ocCurrent,ready)}</td><td>${ready?rateCell(row.ocRate):'<span class="v308-missing">—</span>'}</td><td>${ready&&avg!=null&&Number.isFinite(Number(avg))?`${Number(avg).toFixed(2)}天`:'<span class="v308-missing">—</span>'}</td><td>${cell(row.attempt1Known??row.attempt1,attemptAvailable)}</td><td>${cell(row.attempt2Known??row.attempt2,attemptAvailable)}</td><td>${cell(row.attempt3Known??row.attempt3,attemptAvailable)}</td><td>${cell(row.attemptUnknown,ready)}</td><td>${ready?sampleCell(known,pod):'<span class="v308-missing">—</span>'}</td><td>${ready?sampleCell(signingKnown,pod):'<span class="v308-missing">—</span>'}</td></tr>`;}).join('');panel.insertAdjacentHTML('beforeend',`<div class="v308-table-wrap"><table><thead><tr><th>日期</th><th>总票</th><th>POD</th><th>POD率</th><th>OC</th><th>OC率</th><th>平均签收天数</th><th>1派</th><th>2派</th><th>3派+</th><th>未识别POD</th><th>派次样本</th><th>签收天数样本</th></tr></thead><tbody>${body}</tbody></table></div><div class="v308-foot">TBKH、SHOPEE CN、SHOPEE VN统一口径：每票初始1派；只有Pending/失败后再次START才进入下一派，未再次START不增加派次。单票签收天数=实际POD日期−首次日报锁定日期+1；平均签收天数只统计已完成POD。历史大表和轨迹补核全部在独立子进程执行，网页只读轻量缓存。</div>`);}
    const trend=root.querySelector('.v18-trend-section'),preview=root.querySelector('.v18-detail-preview');if(trend?.parentNode)trend.insertAdjacentElement('afterend',panel);else if(preview?.parentNode)preview.parentNode.insertBefore(panel,preview);else root.appendChild(panel);}
  function publishHistory(type,data,rg){if(!global.__CE_QC_V328_HISTORY_PAYLOADS__)global.__CE_QC_V328_HISTORY_PAYLOADS__=new Map();global.__CE_QC_V328_HISTORY_PAYLOADS__.set(`${type}|${rg.to}`,{at:Date.now(),data,range:{...rg}});global.dispatchEvent(new CustomEvent('ce:v328-history-data',{detail:{type,data,range:{...rg}}}));}
  function watchEvidence(type,rg,repair){clearTimeout(evidenceTimer);const status=String(repair?.status||'').toUpperCase(),key=`${type}|${rg.to}`;if(evidenceWatchKey!==key){evidenceWatchKey=key;evidencePolls=0;evidenceCacheVersion=Number(repair?.cacheVersion||0);}if(!['QUEUED','STARTING','RUNNING'].includes(status)){evidenceWatchKey='';evidencePolls=0;return;}if(evidencePolls>=90)return;evidenceTimer=setTimeout(async()=>{if(activeAttemptType()!==type)return;evidencePolls++;try{const r=await nativeFetch(`/api/v328/evidence-status?businessType=${encodeURIComponent(type)}`,{cache:'no-store',credentials:'same-origin'}),data=await r.json(),version=Number(data?.cacheVersion||0),s=String(data?.status||'').toUpperCase();if(version>evidenceCacheVersion){evidenceCacheVersion=version;lastTableKey='';await loadTable(true);}if(s==='COMPLETED'||s==='FAILED'){evidenceWatchKey='';return;}watchEvidence(type,rg,data);}catch{watchEvidence(type,rg,repair);}},4000);}
  async function loadTable(force=false){installStyle();const type=activeAttemptType(),root=visibleRoot(),rg=selectedRange();if(!type||!root||!rg.from||!rg.to||rg.from>rg.to)return;const autoHistory=rg.from===rg.to,key=`${type}|${rg.from}|${rg.to}|${autoHistory?'H':'E'}`;if(!force&&lastTableKey===key&&root.querySelector('#v308DeliveryDailyTable'))return;let panel=root.querySelector('#v308DeliveryDailyTable');if(!panel){panel=document.createElement('section');panel.id='v308DeliveryDailyTable';panel.className='v18-panel';panel.innerHTML='<div class="v308-state">正在读取派次与签收时效缓存…</div>';const trend=root.querySelector('.v18-trend-section');if(trend)trend.insertAdjacentElement('afterend',panel);else root.appendChild(panel);}else{const state=panel.querySelector('.v308-state');if(state)state.textContent='后台更新中，当前表格保持显示…';}
    try{const historyParam=autoHistory?'&history=all':'',r=await global.fetch(`/api/v308/delivery-daily?businessType=${encodeURIComponent(type)}&from=${encodeURIComponent(rg.from)}&to=${encodeURIComponent(rg.to)}${historyParam}`,{cache:'no-store',credentials:'same-origin'}),raw=await r.text();let data={};try{data=raw?JSON.parse(raw):{};}catch{}if(!r.ok||data?.ok===false)throw new Error(data?.error||data?.message||`HTTP ${r.status}`);if(activeAttemptType()!==type)return;renderTable(root,type,data,rg);lastTableKey=key;if(autoHistory){publishHistory(type,data,rg);watchEvidence(type,rg,data.evidenceRepair||{});}}catch(error){const current=root.querySelector('#v308DeliveryDailyTable');if(current&&!current.querySelector('table'))current.innerHTML=`<div class="v308-state error">派次与签收时效读取失败：${esc(error?.name==='AbortError'?'读取超时':error?.message||error)}。</div>`;}}
  function schedule(ms=250,force=false){clearTimeout(tableTimer);tableTimer=setTimeout(()=>loadTable(force),ms);}function bind(){installStyle();schedule(500,true);document.addEventListener('click',e=>{if(e.target?.closest?.('.side-link[data-page],#topRangeQuery,.top-range-query,#dashboardRangeQuery'))schedule(250,true);},true);document.addEventListener('change',e=>{if(e.target?.matches?.('#topRangeFrom,#topRangeTo,#dashboardRangeFrom,#dashboardRangeTo'))schedule(250,true);},true);global.addEventListener('ce:exact-date-loaded',()=>schedule(120,false));global.addEventListener('popstate',()=>schedule(250,true));global.addEventListener('focus',()=>loadTable(false));}
  if(document.readyState==='loading')document.addEventListener('DOMContentLoaded',bind,{once:true});else bind();global.__CE_QC_V308_DASHBOARD_READ_BRIDGE__={version:VERSION,nativeFetch,rewrite,loadTable};console.info('[CE-QC][V329_DASHBOARD_READ_BRIDGE]',VERSION,'TBKH/CN/VN web reads are cache-only; no history scan in browser requests and no table flashing during background refresh.');
})(window);