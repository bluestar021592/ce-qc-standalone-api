const fs=require('fs');const assert=require('assert/strict');const {execFileSync}=require('child_process');
for(const file of ['src/v328ThreeBusinessHistoryFast.js','src/v329ThreeBusinessDailyCache.js','src/v328EvidenceRepairCoordinator.js','scripts/v329-three-business-cache-worker.mjs','scripts/v328-three-business-evidence-worker-v2.mjs','src/v308DeliveryDailyFastPath.js','public/v308-dashboard-read-bridge.js'])execFileSync(process.execPath,['--check',file],{stdio:'pipe'});
const legacy=fs.readFileSync('src/v328ThreeBusinessHistoryFast.js','utf8'),cache=fs.readFileSync('src/v329ThreeBusinessDailyCache.js','utf8'),coordinator=fs.readFileSync('src/v328EvidenceRepairCoordinator.js','utf8'),worker=fs.readFileSync('scripts/v329-three-business-cache-worker.mjs','utf8'),backend=fs.readFileSync('src/v308DeliveryDailyFastPath.js','utf8'),ui=fs.readFileSync('public/v308-dashboard-read-bridge.js','utf8');
assert.match(legacy,/NULLIF\(ledgerPodDate,''\)/,'legacy isolated builder still requires real POD evidence');assert.doesNotMatch(legacy,/CASE WHEN finalPod=1 THEN NULLIF\(latestEventTime,''\) ELSE '' END/,'ordinary latestEventTime cannot impersonate POD');assert.match(cache,/实际POD日期-首次日报锁定日期\+1|actual POD|首次日报锁定日期/);assert.match(cache,/signingComplete/);assert.match(worker,/l\.signingDays/);assert.match(worker,/firstReportDate/);assert.match(worker,/inclusive\(start,p\)/);assert.match(coordinator,/v329-three-business-cache-worker\.mjs/);assert.doesNotMatch(backend,/readV328ThreeBusinessHistory/,'main web route must not run the heavy legacy builder');assert.match(ui,/平均签收天数=实际POD日期−首次日报锁定日期\+1/);assert.match(ui,/只有Pending\/失败后再次START才进入下一派/);console.log('[V329] metric guard passed · visible average=first report lock→actual POD inclusive · strict attempts unchanged · heavy history builder isolated from web process');