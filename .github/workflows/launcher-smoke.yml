name: CE QC launcher smoke

on:
  pull_request:
    branches:
      - main

jobs:
  launcher:
    runs-on: windows-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - name: Parse launcher scripts with Windows PowerShell 5.1
        shell: powershell
        run: |
          $files = @('tools/CE_QC_Start.ps1','tools/Install_CE_QC_Desktop_Shortcut.ps1')
          foreach ($file in $files) {
            $tokens = $null
            $errors = $null
            [System.Management.Automation.Language.Parser]::ParseFile((Resolve-Path $file), [ref]$tokens, [ref]$errors) | Out-Null
            if ($errors.Count -gt 0) {
              $errors | Format-List | Out-String | Write-Error
              exit 1
            }
            $bytes = [System.IO.File]::ReadAllBytes((Resolve-Path $file))
            if (@($bytes | Where-Object { $_ -gt 127 }).Count -gt 0) {
              throw "$file must remain ASCII-only for Windows PowerShell 5.1 compatibility"
            }
          }
      - name: Verify stable launcher files
        shell: powershell
        run: |
          if (-not (Test-Path 'CE_QC_Start.bat')) { throw 'CE_QC_Start.bat missing' }
          if (-not (Test-Path 'Install_CE_QC_Desktop_Shortcut.bat')) { throw 'shortcut installer missing' }
          $text = Get-Content 'tools/CE_QC_Start.ps1' -Raw
          foreach ($required in @(
            'fetch','origin','main','reset','--hard','origin/main','node_modules','bootstrap.js','5177',
            'Get-PortOwnerPids','Stop-PortOwners','taskkill.exe','/T','/F','Start-CeQcBackend','Get-NetTCPConnection','netstat -ano'
          )) {
            if (-not $text.Contains($required)) { throw "launcher missing required token: $required" }
          }
          if ($text -notmatch 'launchAttempt\s*=\s*1[\s\S]*launchAttempt\s*-le\s*2') {
            throw 'launcher must retry backend startup once after a port collision'
          }
          $installer = Get-Content 'tools/Install_CE_QC_Desktop_Shortcut.ps1' -Raw
          foreach ($required in @('0x8D28','0x63A7','0x542F','0x52A8','CE_QC_Start.bat')) {
            if (-not $installer.Contains($required)) { throw "installer missing Unicode-safe shortcut token: $required" }
          }
