name: Apply CCSL waybill resume patch

on:
  pull_request:
    branches:
      - main
    paths:
      - 'scripts/apply-ccsl-waybill-resume-patch.mjs'
      - '.github/workflows/apply-ccsl-waybill-resume-patch.yml'

permissions:
  contents: write

jobs:
  patch:
    if: github.event.pull_request.head.repo.full_name == github.repository
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
        with:
          ref: ${{ github.event.pull_request.head.ref }}
          fetch-depth: 0
      - uses: actions/setup-node@v4
        with:
          node-version: '22'
      - run: node scripts/apply-ccsl-waybill-resume-patch.mjs
      - name: Commit CCSL resume integration
        shell: bash
        run: |
          if git diff --quiet -- src/pipeline.js; then
            echo 'CCSL resume already patched'
            exit 0
          fi
          git config user.name 'github-actions[bot]'
          git config user.email '41898282+github-actions[bot]@users.noreply.github.com'
          git add src/pipeline.js
          git commit -m 'Resume CCSL from per-waybill scan and track status'
          git push origin HEAD:${{ github.event.pull_request.head.ref }}
